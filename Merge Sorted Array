class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        if (n==0) return;
        int count=m+n;
        int i=0;
        while(m<count){
            nums1[m]=nums2[i];
            m++;
            i++;
        }
        Arrays.sort(nums1);
    }
}
