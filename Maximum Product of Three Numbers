class Solution {
    public int maximumProduct(int[] arr) {
        Arrays.sort(arr);
        int n=arr.length;
        int res=arr[0]*arr[1]*arr[n-1];
        int result=arr[n-1]*arr[n-2]*arr[n-3];
          int maxi=Math.max(result,res);
        return maxi;
    
    }
}
